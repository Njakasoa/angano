{
  "name": "angano-e2e",
  "private": true,
  "type": "module",
  "description": "Angano end-to-end & protocol tests (kept out of the app build).",
  "scripts": {
    "smoke": "bun smoke.ts",
    "reconnect": "bun reconnect.ts",
    "story": "bun story.ts",
    "voice": "bun voice.ts",
    "scenarios": "node scenarios.mjs",
    "test:proto": "bun smoke.ts && bun reconnect.ts && bun story.ts && bun voice.ts",
    "test": "bun smoke.ts && bun reconnect.ts && bun story.ts && bun voice.ts && node scenarios.mjs"
  },
  "devDependencies": {
    "playwright": "^1.49.0"
  }
}
